<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><meta name="referrer" content="no-referrer"><title>Application {{ $applicationPortal['application_number'] }}</title><style>body{font-family:system-ui,sans-serif;background:#f4f6f8;margin:0;color:#20242a}.wrap{max-width:720px;margin:auto;padding:20px}.card{background:#fff;border-radius:12px;padding:18px;margin-bottom:14px;box-shadow:0 1px 4px #0002}.muted{color:#667085}textarea,button{width:100%;box-sizing:border-box;padding:10px;border:1px solid #ccd2d8;border-radius:8px;margin-top:8px}button{background:#20242a;color:#fff}</style></head><body><main class="wrap">
<div class="card"><h1>{{ $applicationPortal['listing_title'] ?: 'Premises application' }}</h1><div>{{ $applicationPortal['applicant_name'] }}</div><div class="muted">{{ $applicationPortal['application_number'] }} · {{ str($applicationPortal['status'])->replace('_',' ')->title() }}</div></div>
@if(session('success'))<div class="card">{{ session('success') }}</div>@endif
<div class="card"><h2>Progress</h2>@forelse($applicationPortal['events'] as $event)<div style="margin-bottom:10px"><strong>{{ $event->summary }}</strong><div class="muted">{{ optional($event->occurred_at)->format('d M Y H:i') }}</div></div>@empty<div class="muted">Your application has been received.</div>@endforelse</div>
<div class="card"><h2>Messages</h2>@forelse($applicationPortal['messages'] as $message)<div style="margin-bottom:12px"><strong>{{ $message->direction === 'inbound' ? 'You' : 'Manager' }}</strong><div>{{ $message->body }}</div><div class="muted">{{ optional($message->sent_at)->format('d M Y H:i') }}</div></div>@empty<div class="muted">No messages yet.</div>@endforelse
<form method="POST" action="{{ route('managedpremises.public.applications.messages.store', $token) }}">@csrf<textarea name="body" maxlength="10000" rows="4" required placeholder="Send a message about your application"></textarea><button type="submit">Send message</button></form></div>
<div class="muted" style="font-size:12px;text-align:center">Keep this private link secure.</div>
</main></body></html>
