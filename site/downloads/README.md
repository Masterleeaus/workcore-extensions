# Generated release downloads

Release ZIPs are deterministic build outputs and are intentionally not committed to Git history.
Run `python tools/build_extensions.py` and `python tools/build_site.py` to regenerate them.
