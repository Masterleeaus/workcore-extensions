# Generated release downloads

Release ZIPs are deterministic build outputs and are intentionally not committed here.
Run `python tools/build_extensions.py` against the consolidated source to regenerate them.
