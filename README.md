# WorkCore Extensions

Manifest-driven extraction of the consolidated WorkCore codebase into five domain extension groups plus a shared foundation.

Active development branch: `feature/five-domain-extension-split`.
