# WorkCore Agent 1 Remainder — Rebuilt Split

Contains all **5,873 original files** excluded from the canonical 1,999-file source set.

Nothing was deleted. Paths are preserved under `original-relative-paths/`.

The manifests reconcile all **7,872 original files** with zero overlap and zero loss.
